<%= Navigation("clubsessions") %>

<div class="content">
	<h1>Editing session</h1>

	<%= render 'form' %>

	<%= link_to 'Show', @session, :class  => "blue-pill" %>
	<%= link_to 'Back', sessions_path, :class  => "blue-pill" %>
</div>
